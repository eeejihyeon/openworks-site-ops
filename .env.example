# JWT 서명 비밀키 (필수 - 랜덤 문자열로 변경하세요)
JWT_SECRET=your-super-secret-key-change-this

# SQLite DB 파일 경로
# Railway 볼륨을 /data 에 마운트한 경우 아래 경로 사용
DB_PATH=/data/facility.db

# Node 환경 (Railway에서는 production으로 설정)
NODE_ENV=production

# 서버 포트 (Railway가 자동으로 주입 - 별도 설정 불필요)
# PORT=4000
